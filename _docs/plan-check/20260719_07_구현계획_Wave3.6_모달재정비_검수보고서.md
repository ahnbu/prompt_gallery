---
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-07-19 17:59:54"
---

이 계획 문서를 면밀히 검토한 결과입니다.

### 1. 총평 및 목적 달성 가능성
**목적 달성 가능성: 높음**
Wave 3.5에서 놓친 헤더와 본문의 제목 이중 렌더링 문제를 정확히 짚어내고 있으며, 카드 UI와의 일관성을 확보하는 방향으로 잘 설계되었습니다. 다만, 편집 모드에서 **새로운 형태의 이중 렌더링(UI 중복)**을 유발하는 치명적인 결함이 있어 수정이 필요합니다.

### 2. 크리티컬 결함 및 리스크 (직언)
* **편집 모드에서의 제목 UI 이중 노출**:
  * **문제**: [관심사 1-1]에서 헤더 H2에 `draft.title`을 실시간 반영하도록 변경하고, [관심사 3-1]에서 편집 폼(ItemModalForm) 최상단에 **제목 입력칸**을 두도록 규정했습니다. 이는 편집 모드 진입 시 상단 헤더에 제목(Text)이 나오고, 그 바로 아래 폼에 제목(Input)이 또 나오는 **새로운 제목 중복 현상**을 만듭니다.
  * **해결 제안**: 모달 헤더에 제목 텍스트를 노출할 것이라면 폼 안의 Input을 제외(또는 헤더 자체를 Input으로 변경)하거나, 기존처럼 헤더에는 "항목 편집" 같은 메타 라벨만 두고 본문 폼 안에서만 제목을 편집/노출하도록 규칙을 수정해야 합니다.

### 3. DESIGN.md 충돌 지적
* `DESIGN.md`의 **289~290행**("Detail body is the primary content, followed by a divider, subdued title...") 명세와 이 계획의 [관심사 2]가 정면으로 충돌합니다.
* 계획은 하단의 subdued title을 삭제하고 헤더로 제목을 올리는 구조 변경을 확정했으므로, **계획 범위에 `DESIGN.md` 문서 갱신 작업이 반드시 포함**되어야 합니다. 그렇지 않으면 이후 개발에서 가이드라인과 구현이 불일치하는 Context 훼손이 발생합니다.

### 4. 검증 기준 및 QA 스크립트 점검
* **증거 기반 판정 (완료 기준)**:
  * 헤드리스 실행 명시(`qa:browser(headless)`), 종료코드(`0`), 증거물 저장 경로(`.omo/evidence/`), `gitignore` 처리 등 done-check가 사람 눈 없이 판정할 수 있는 모든 요건이 완벽하게 갖춰져 있습니다. 목적과 완료 기준 간의 모순도 없습니다.
* **QA 스크립트 작성 지침의 부족 (실행 불가 리스크)**:
  * `browser-wave36.mjs`라는 신규 스크립트를 작성한다고 언급(119행)되어 있으나, 이를 쌩(Raw) Playwright로 작성할지 시스템 헬퍼(예: `hidden-browser-verify-runner.mjs`)를 재사용할지 명시되지 않았습니다.
  * 이 부분이 비어있으면 AI가 불필요하게 방대한 보일러플레이트 코드를 작성하는 **과최적화**가 발생할 수 있습니다. 스크립트 작성 시 기존 헬퍼 재사용 및 검증 로직 작성 지침을 구체화해야 합니다.

### 5. 트레이드오프 (접근법의 명암)
* **복사 버튼 역할 다중화 (관심사 1-2)**:
  * 모달과 카드의 복사 버튼 하나로 프롬프트 내용(body)과 레포 링크(githubUrl)를 모두 처리하는 접근법입니다.
  * **장점**: UI를 늘리지 않고 깔끔하게 유지(D1 표준 유지 규칙 충족).
  * **단점**: 사용자가 아이템 타입(프롬프트 vs 레포)을 인지하지 못한 상태로 복사 버튼을 눌렀을 때 클립보드에 들어가는 내용물(텍스트 vs URL)이 달라 직관성이 떨어질 수 있습니다.
* **파비콘 폴백 제거**:
  * 계획(110행)에 적힌 대로, 구형 브라우저에서 아이콘이 깨지는 것을 감수하고 .ico 변환 공수를 줄이는 합리적인 트레이드오프입니다.

### 요약 및 다음 행동 제안
이 계획은 전반적으로 매우 우수하나, **1) 편집 모드 제목 UI 중복 문제 해결**, **2) DESIGN.md 갱신 작업 추가**, **3) QA 스크립트 헬퍼 재사용 지침 추가** 3가지를 반영하도록 Plan 문서를 먼저 업데이트(수정)한 후 구현에 착수할 것을 강력히 권장합니다.

---

## 현재 AI 코드베이스 검증 결과 (claude)

reviewer(agy) 지적 3건을 코드로 직접 확인함.

### 지적 1 — 편집 모드 제목 UI 중복: **부분 확증 + 승인 필요**
- 코드 확인: 계획 45행은 편집 모드 헤더 h2에 `draft.title` 실시간 반영을 규정. 폼([ItemModalForm.tsx:58-65](../src/client/ItemModalForm.tsx)) 최상단에 제목 input 존재. 계획 적용 시 헤더 제목 텍스트 + 폼 제목 input이 동시 노출되는 것은 **사실**.
- 그러나 세션 ce814896에서 사용자와 어시스턴트가 이 처리를 합의함: "'편집' 없이 뱃지+실제 제목만... 실제 제목(또는 편집 폼 제목 입력값)을 그대로 보여주는 게 낫다" + 확정 대상 4번 "편집 폼 제목을 최상단으로". 즉 **헤더 제목 텍스트 + 폼 제목 input 병존은 사용자가 인지·승인한 설계**.
- 단, detail 모드의 "동일 텍스트 2회 렌더" 중복과 달리, edit 모드는 (헤더=읽기 텍스트 / 폼=편집 input)으로 성격이 다름. 시각적으로 제목이 두 번 보이는 점을 사용자가 최종 확정했는지는 불명확 → **사용자 판단 필요(승인 필요)**.

### 지적 2 — DESIGN.md 충돌: **확증 (계획 범위 보강 필요)**
- [DESIGN.md:290](../DESIGN.md) "Detail body is the primary content, followed by a divider, subdued title, tags, and quiet date metadata"가 계획 관심사 2(제목 헤더 이동 + 하단 title-section 삭제)와 정면 충돌.
- 계획 관심사 1~4 어디에도 DESIGN.md 갱신 언급 없음. 문서-구현 불일치를 막으려면 DESIGN.md 5장 Modals 절 갱신을 범위에 추가해야 함. **타당**.

### 지적 3 — browser-wave36.mjs 헬퍼 재사용 지침: **확증 (권고 수준)**
- 계획 109행이 신규 스크립트만 언급, 작성 방식 미명시.
- 다만 기존 [browser-wave35.mjs](../scripts/qa/browser-wave35.mjs)가 강한 패턴 확립: `chromium`(headless) + `seedGalleryData`(browser-gallery-fixtures.mjs) + `screenshot`(browser-image-preview-support.mjs) + `startLocalApp/stopProcess`(browser-smoke-support.mjs). 이 패턴 답습이면 보일러플레이트 리스크 낮음. 계획에 "browser-wave35.mjs 패턴 재사용" 한 줄 명시 권고.

### 종합 판정 (one-stop 게이트)
- **승인 필요**: 지적 1(편집 제목 병존)은 사용자 최종 승인이 필요하고, 지적 2(DESIGN.md 갱신)는 확정 구현 범위 확장에 해당하므로 사용자 승인 없이 현재 AI가 임의 반영 불가.
