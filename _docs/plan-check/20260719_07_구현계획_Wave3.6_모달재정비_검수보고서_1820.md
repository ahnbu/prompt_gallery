---
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-07-19 18:20:37"
---

**재검수 결과: 계획 보완 필요 (크리티컬 결함 1건 발견)** → 현재 AI가 반영 완료 → **실행 가능**

이전 검수에서 지적된 3가지 항목(제목 UI 중복 제거안 확정, DESIGN.md 갱신 추가, QA 헬퍼 재사용 명시)은 계획에 모두 적절하게 반영되었습니다. 하지만 편집 헤더에서 제목 h2를 제거하는 B안을 `ItemModal.tsx`에 적용할 때 발생하는 **접근성(a11y) 부작용이 계획에서 누락**되었습니다.

### 🔴 발견된 결함 (수정 필수)

- **결함 내용**: `ItemModal.tsx` 159행의 `<dialog>` 요소는 `aria-labelledby="item-modal-title"`을 하드코딩으로 가지고 있습니다. 계획(관심사 1-1)대로 `detail` 모드에서만 `<h2 id="item-modal-title">`를 렌더링하고 `edit/add` 모드에서는 렌더링하지 않으면, 편집 모드일 때 **참조할 대상 ID가 없는 고아(orphaned) `aria-labelledby` 속성**이 되어 접근성 표준을 위반하게 됩니다.
- **해결 대안 (계획에 추가 필요)**: `<dialog>`의 접근성 속성을 모드에 따라 동적으로 분기해야 합니다. detail 모드는 `aria-labelledby="item-modal-title"` 유지, edit/add 모드는 `aria-labelledby` 생략 + `aria-label` 부여.

### ✅ 이전 지적사항 반영 확인
1. **편집 모드 제목 UI 중복 (B안 적용)** — 반영됨.
2. **DESIGN.md 갱신 지침** — 충분함.
3. **QA 스크립트 헬퍼 재사용** — 충분함.

### 🔎 기타 검증 관점
- **완료기준 및 검증방법 (DC-A~F)**: 증거 기반으로 구체적, 자동화된 done-check 가능.
- **headless 실행 및 증거 처리**: 닫힌 프로세스로 구성됨.
- **과최적화/모순**: 확인되지 않음.

**권장 액션**: `aria-labelledby` 동적 처리 방안을 관심사 1-1 계획에 추가한 뒤 구현 시작.

---

## 현재 AI 코드베이스 검증 + 반영 결과 (claude)

### aria-labelledby 고아 참조 결함 — **확증 후 계획 반영 완료**
- 코드 확인: [ItemModal.tsx:159](../src/client/ItemModal.tsx) `<dialog aria-labelledby="item-modal-title">` 하드코딩. [ItemModal.tsx:183](../src/client/ItemModal.tsx) `<h2 id="item-modal-title">` — B안대로면 edit/add 모드에서 이 h2 소멸 → 고아 참조. **결함 실재 확증.**
- 반영: 관심사 1-1에 하위 3번(접근성 부작용 처리) 추가 — detail은 `aria-labelledby` 유지, edit/add는 `aria-label`(add="새 항목", edit=item.title). 완료기준·검증(4)에 dialog aria assertion 추가.

### 종합 판정 (one-stop 게이트): **실행 가능**
- 1차 지적 3건(편집 제목 중복/DESIGN.md/QA 헬퍼) 모두 계획 반영 완료.
- 2차 지적 1건(aria 고아 참조) 코드 확증 후 계획 반영 완료.
- 잔여 크리티컬 결함·승인 필요·확인 불가 항목 없음. 완료기준·검증이 done-check 증거 기반 판정 가능 수준.
