# BACKLOG — 후속 작업 후보

현재 작업 범위 밖이지만 나중에 가치 있는 작업을 기록합니다. 새 항목은 표 최하단에 추가합니다.

## BACKLOG 관리 규칙

### 추가 기준 (다음 조건 중 하나 이상)

- 지금 당장 실행하지 않지만 나중에 가치 있는 버그·개선·기술부채
- 발견된 문제 중 현재 작업 범위 밖인 것
- 다음 세션에서 이어받아야 할 미완료 작업

### 제외 기준 (추가하지 않음)
- 사소한 스타일 정리, 타입 힌트 추가 등 임팩트 낮은 것
- 이미 완료된 작업
- 즉시 실행해야 할 것 (바로 처리하거나 TODO 주석으로)

### 상태 표기 규칙
- **완료 항목**: 표에서 행을 삭제한다. 취소선(~~텍스트~~) 사용 금지. 히스토리는 git log로 추적.
- **보류 항목**: 항목명 앞에 `[보류]` 접두어를 붙인다. 명시적 지시가 있기 전까지 착수하지 않는다.
- **일반 항목**: 접두어 없음. 우선순위에 따라 착수 가능.

### ID 규칙
- 형식: `BL-MMDD-NN` (MM=월, DD=일, NN=당일 순번 01부터)
- 한 번 부여된 ID는 변경·재사용하지 않는다 (삭제해도 번호 유지)
- 외부 문서에서 참조 시: `BL-0412-01` 형태로 사용

## 공용 템플릿

| ID         | 날짜         | 항목                          | 위치                                                   | 실행 용이성   | 기대효과     | 메모                                                                                                                                       |
| ---------- | ---------- | --------------------------- | ---------------------------------------------------- | -------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| BL-0708-01 | 2026-07-08 | 이미지 업로드 비용 방어 스펙 별도 구현계획 수립 | [[prompt-gallery-implementation]] Wave 3 | `████░░` | `██████` | [[20260708_04_R2-비용폭주-방지-의사결정]] 기준. 원본 10MB 제한, 1600px preview 압축, WebP 우선/JPEG fallback을 별도 승인 후 Wave로 반영. 클라이언트 압축 유틸, 실패 처리, Worker asset API 검증, Playwright QA까지 포함해야 함. |
| BL-0709-01 | 2026-07-09 | 워크플로우 즐겨찾기 지원 | `src/worker/workflow-*`, `src/client/WorkflowModal*`, `src/client/GalleryCard.tsx` | `██░░░░` | `████░░` | 현재 `WorkflowItem`에는 `favorite` 필드와 전용 토글 API가 없음. 별도 구현 시 `workflows` 저장 구조, `/api/workflows/:id/favorite`, 즐겨찾기 탭 포함 여부, 카드·상세 액션, QA 시나리오를 함께 설계해야 함. |

> **컬럼 기준** — `실행 용이성`: `██████` 쉬움 / `████░░` 보통 / `██░░░░` 어려움 | `기대효과`: `██████` 높음 / `████░░` 보통 / `██░░░░` 낮음. 두 값의 합이 클수록 우선순위 높음. AI 초안이므로 실행 전 재평가 권장.
